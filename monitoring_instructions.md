# Setting up Flutter DevTools for monitoring

1. Run your app with DevTools enabled:
   ```
   flutter run --devtools
   ```

2. Open the DevTools URL that appears in your terminal

3. Use the Performance tab to monitor:
   - Frame rendering times
   - CPU usage
   - Memory allocation

4. Use the Logging tab to watch for errors